import { MatSymbolDirective } from './mat-symbol.directive';

describe('MatSymbolDirective', () => {
  it('should create an instance', () => {
    const directive = new MatSymbolDirective();
    expect(directive).toBeTruthy();
  });
});
